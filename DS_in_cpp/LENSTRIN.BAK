#include<stdio.h>
#include<conio.h>

void main(){
int i,loop=0;
char str[50];
clrscr();
printf("Enter a String: ");
gets(str);
for(i=0;str[i]!='\0';i++){loop++;}
//121519
printf("Length: %d\nLoop: %d",i,loop);
getch();
}